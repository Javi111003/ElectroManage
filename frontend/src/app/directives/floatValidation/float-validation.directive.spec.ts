import { FloatValidationDirective } from './float-validation.directive';

describe('FloatValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new FloatValidationDirective([][0]);
    expect(directive).toBeTruthy();
  });
});
