import { MathValidatorDirective } from './math-validator.directive';

describe('MathValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new MathValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
