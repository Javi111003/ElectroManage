import { ShowForRolesDirective } from './show-for-roles.directive';

describe('ShowForRolesDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowForRolesDirective([][0], [][0], [][0]);
    expect(directive).toBeTruthy();
  });
});
